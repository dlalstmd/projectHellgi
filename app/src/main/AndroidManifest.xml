<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.projecthellgi" >

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ProjectHellgi"
        tools:targetApi="31" >
        <activity
            android:name=".MainActivity"
            android:exported="true" android:theme="@style/Theme.AppCompat.DayNight.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Register_Page" />
        <activity android:name=".Login_page" />
        <activity
            android:name=".Register_Page"
            tools:ignore="DuplicateActivity" />
        <activity android:name=".HomeActivity_page" />
        <activity android:name=".Profile_page" />
        <activity android:name=".Message_Page" />
        <activity android:name=".ChatActivity" />
        <activity android:name=".ChatMainActivity"/>
        <activity android:name=".AddPhoto_page"/>
        <activity android:name=".WorkChange_page"/>
    </application>

</manifest>